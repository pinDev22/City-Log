import React from 'react';

const City = () => {
  return (
    <div>
      <h2>Enter what city you currently live in:</h2>
      <input type="text" placeholder="City name" />
      <button>Enter</button>
    </div>
  );
};

export default City;
